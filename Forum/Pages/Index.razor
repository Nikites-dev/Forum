@page "/"
@using Forum.Services
@inject BehaviourService BehaviourService 
<PageTitle>Forum</PageTitle>

<div class="fon">
    ㅤ
</div>

<div class="page">
    <HeaderMenu/>
    <div class="contentMain">
        <div class="sideLeft">
            <NavMenu onDataChanged="@OnFilterChanged"></NavMenu>
        </div>
        
        <div class="sideRight">
           @* <PostContent Filter="@Filter" RegUser="null" SortName=""></PostContent> *@
          <PostContent Filter="@Filter" RegUser="null" SortName="@Filter" IsProflie="false" onDataChanged="@OnFilterChanged"></PostContent>
        </div>
        
    </div>
</div>


@code {
    private string Filter { get; set; }
    protected override void OnInitialized()
    {
        BehaviourService.isProfile = false;
        BehaviourService.SelectInterest = "";
    }

    public void OnFilterChanged(string newValue)
    {
        Filter = newValue;
        StateHasChanged();
    }
}